-- Colours (for lovers of british spelling)
if not colors then
  os.loadAPI("rom/apis/colors")
end
for k,v in pairs(colors) do
	_ENV[k] = v
end

_ENV.grey = colors.gray
_ENV.gray = nil

_ENV.lightGrey = colors.lightGray
_ENV.lightGray = nil