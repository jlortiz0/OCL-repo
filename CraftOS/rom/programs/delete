
local tArgs = { ... }
if #tArgs < 1 then
	print( "Usage: rm <path>" )
	return
end

local sPath = shell.resolve( tArgs[1] )
--Until I figure out fs.find, I'll have to use this
if fs.exists(sPath) then
  fs.delete(sPath)
else
  printError("No such file.")
end